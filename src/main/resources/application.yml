server:
  port: 8080
  error:
    whitelable:
      enabled: false
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/lutiy-project
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
